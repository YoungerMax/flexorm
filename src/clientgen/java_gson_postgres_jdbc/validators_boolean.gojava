{{- if or .Nullable .HasDefault}}
        if (obj.{{.ColName}} != null) {
{{else}}
        if (obj.{{.ColName}} == null) {
            throw new IllegalArgumentException("{{.ColName}} is required");
        } else {
{{- end}}
            if (!(obj.{{.ColName}} instanceof Boolean)) {
                throw new IllegalArgumentException("{{.ColName}} must be a Boolean");
            }
        }