{{- if or .Nullable .HasDefault}}
        if (obj.{{.ColName}} != null) {
            if (!(obj.{{.ColName}} instanceof java.time.LocalDate)) {
                throw new IllegalArgumentException("{{.ColName}} must be a LocalDate");
            }
        }
{{else}}
        if (obj.{{.ColName}} == null) {
            throw new IllegalArgumentException("{{.ColName}} is required");
        }
        if (!(obj.{{.ColName}} instanceof java.time.LocalDate)) {
            throw new IllegalArgumentException("{{.ColName}} must be a LocalDate");
        }
{{- end}}