{{- if or .Nullable .HasDefault}}
        if (obj.{{.ColName}} != null) {
            if (!(obj.{{.ColName}} instanceof java.util.UUID)) {
                throw new IllegalArgumentException("{{.ColName}} must be a UUID");
            }
        }
{{else}}
        if (obj.{{.ColName}} == null) {
            throw new IllegalArgumentException("{{.ColName}} is required");
        }
        if (!(obj.{{.ColName}} instanceof java.util.UUID)) {
            throw new IllegalArgumentException("{{.ColName}} must be a UUID");
        }
{{- end}}